/**
 * Regex for parsing the calendar string
 * Must return an array with two elements: the name of the calendar and the name of the room
 */
export const CALENDAR_PARSING_REGEX = /^([^@]+)@([^@]+)$/
