# DOCS

## Swagger Autogeneration

The command `npm run pdf` will automatically generate a API-documentation for the backend. 
It will create a Swagger 2.0 json file (`swagger.json`) and a PDF showing the endpoints (`swagger.pdf`).